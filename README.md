# Library

A simple library program that lets users store books (without persistent storage). Users can add new books, change the read status of old books, and delete books, and stats are shown about what the user has read. 